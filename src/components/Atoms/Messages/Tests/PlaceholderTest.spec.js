/* eslint-disable no-undef */
import { mount } from '@vue/test-utils';
import { h } from 'vue';

describe('Placeholder test', () => {
    it('Stuff for in case no tests present', () => {
        expect(true);
    });
});
